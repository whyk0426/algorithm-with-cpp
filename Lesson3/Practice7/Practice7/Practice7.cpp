#include <iostream>
#include <vector>

class BloomFilter {
private:
    std::vector<bool> data;
    int N;

public:
    BloomFilter(int n) : N(n) {
        data = std::vector<bool>(n, false);
    }


};

int main()
{
    std::cout << "Need to download for conclude!!!\n";
}